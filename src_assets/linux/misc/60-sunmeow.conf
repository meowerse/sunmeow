# MEOW-TOUCH(rebrand): renamed from 60-sunshine.conf, which the distro package owns at the
# same path in /usr/lib/modules-load.d/. Sunmeow needs uhid for descriptor-driven gamepad
# emulation (upstream reworded this from "DS5 emulation" in c75d5d76; libvirtualhid now
# drives more than the DS5, so the old wording understated it).
uhid
